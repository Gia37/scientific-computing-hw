# Gisela Arrieta Rivera
# 

import numpy as np
import matplotlib
import matplotlib.pyplot as plt
import sys

np.random.seed(0)
for j in range(7):	
	print np.random.random()



R = 1
L = 3
N = 100
N1 = 1000
N2 = 10000
pi = (L/R)**2 * (n/N)


for n in range(-1.5,1.5):
	if x: np.sqrt(X**2) + (Y**2)<=R
	print x + 1

	else y:np.sqrt(X**2) + (Y**2)<=R
	print y + 1

	
